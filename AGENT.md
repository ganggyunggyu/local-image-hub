# AGENT.md

## 목적
- local-image-llm의 생성 결과를 빠르게 탐색/재사용할 수 있게 유지보수함.

## 작업 원칙
- 기존 생성 API(`/api/generate`) 동작을 깨지 않는 범위에서 확장함.
- 결과물은 `outputs/` 기준으로 관리하고, 메타는 DB/sidecar로 추적 가능하게 만듦.
- 대량 파일 처리 시 재실행 안전(idempotent)하게 구현함.

## 우선순위
1. 메타 인덱싱 + 검색 API
2. 생성 시 메타 자동 기록
3. 갤러리 UI/필터
4. 중복/유사 이미지 정리
